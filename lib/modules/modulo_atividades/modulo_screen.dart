import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'modulo_controller.dart';

class ModuloScreen extends StatelessWidget {
  const ModuloScreen({Key? key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}

